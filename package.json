{
  "name": "html-only",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && minify ./dist/index.html > ./dist/index-zip.html && node readAndEscape.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.1"
  },
  "dependencies": {
    "minify": "^11.4.0",
    "vite-plugin-singlefile": "^2.0.2"
  }
}
